import ComponentRenderer from './component-renderer'

describe('@components/component-renderer', () => {
  it('exports a valid component', () => {
    expect(ComponentRenderer).toBeAComponent()
  })
})
